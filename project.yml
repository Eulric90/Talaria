name: Talaria
options:
  # Public-safe defaults. Replace locally if you regenerate the project
  # for a private deployment with your own signing/bundle identifiers.
  bundleIdPrefix: io.github.eulric90
  deploymentTarget:
    iOS: "26.0"
  xcodeVersion: "26.3"
  defaultConfig: Debug
  generateEmptyDirectories: true
  groupSortPosition: top
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "6.2"
    SWIFT_STRICT_CONCURRENCY: complete
    IPHONEOS_DEPLOYMENT_TARGET: "26.0"
    TARGETED_DEVICE_FAMILY: "1,2"
    INFOPLIST_KEY_UILaunchScreen_Generation: true
    # Orientations are declared in the app target's `info.properties` below,
    # NOT here: INFOPLIST_KEY_* build settings only reach the bundle when
    # GENERATE_INFOPLIST_FILE is on, and the app target ships a full custom
    # Info.plist. The old INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone
    # "portrait lock" was silently inert — the app has always rotated (#68).
    ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
    ENABLE_PREVIEWS: true

packages:
  WebRTC:
    url: https://github.com/stasel/WebRTC.git
    version: 130.0.0

targets:
  Talaria:
    type: application
    platform: iOS
    entitlements:
      path: Talaria/Talaria.entitlements
      properties:
        # No aps-environment: Push Notifications requires a paid Apple
        # Developer Program membership and isn't available to a free
        # personal-team signing identity (sideload builds). Notifications
        # already don't work per the README, so nothing lost here.
        com.apple.developer.healthkit: true
        com.apple.developer.healthkit.access: []
        com.apple.developer.healthkit.background-delivery: true
        com.apple.security.application-groups:
          - group.io.github.eulric90.talaria
    sources:
      - path: Talaria
        excludes:
          - "**/.DS_Store"
      # Theme palette tables shared with the widget extension (single source
      # of truth for theme/accent color values).
      - path: Shared
    resources:
      - path: Talaria/Resources
        optional: true
    dependencies:
      - package: WebRTC
      - target: TalariaWidgets
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.github.eulric90.talaria
        PRODUCT_NAME: "Talaria for Hermes Desktop"
        # Keep the Swift module importable as `Talaria` (tests use
        # `@testable import Talaria`); PRODUCT_NAME alone would rename the
        # module to Talaria_for_Hermes_Desktop.
        PRODUCT_MODULE_NAME: Talaria
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        DEVELOPMENT_TEAM: ""  # set to your own free Apple ID personal-team ID if building signed locally in Xcode
        CODE_SIGN_STYLE: Automatic
        INFOPLIST_KEY_CFBundleDisplayName: Talaria
    scheme:
      testTargets:
        - TalariaTests
    info:
      path: Talaria/Resources/Info.plist
      properties:
        CFBundleDisplayName: Talaria
        CFBundleShortVersionString: "1.0.0"
        CFBundleVersion: "1"
        UILaunchScreen: {}
        # Landscape is intentional now (#68) — declare what the app actually
        # does instead of the inert portrait lock (see settings.base note).
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        "UISupportedInterfaceOrientations~ipad":
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        NSLocationWhenInUseUsageDescription: "Hermes uses your location to provide contextual recommendations and nearby suggestions."
        NSHealthShareUsageDescription: "Hermes reads your health data to offer personalized wellness insights."
        NSHealthUpdateUsageDescription: "Hermes does not write health data, but this permission is required by the system."
        NSCameraUsageDescription: "Hermes uses the camera to capture photos and documents for analysis."
        NSPhotoLibraryUsageDescription: "Hermes accesses your photo library to help organize and search your photos."
        NSMicrophoneUsageDescription: "Hermes uses the microphone for voice conversations in Talk Mode."
        NSLocationAlwaysAndWhenInUseUsageDescription: "Hermes uses your location in the background to provide contextual awareness and proactive suggestions."
        NSMotionUsageDescription: "Hermes uses motion data to understand your current activity for contextual awareness."
        NSSpeechRecognitionUsageDescription: "Hermes uses speech recognition for voice commands and local transcription."
        NSSupportsLiveActivities: true
        NSSupportsLiveActivitiesFrequentUpdates: true
        UIAppFonts:
          - ChakraPetch-Medium.ttf
          - ChakraPetch-SemiBold.ttf
          - ChakraPetch-Bold.ttf
          - SpaceGrotesk-Regular.ttf
          - SpaceGrotesk-Medium.ttf
          - SpaceGrotesk-Bold.ttf
          - JetBrainsMono-Regular.ttf
          - JetBrainsMono-Medium.ttf
          - JetBrainsMono-Bold.ttf
        # Alternate home-screen app icons (issue #25). The primary icon stays in
        # the asset catalog; alternates are loose flat PNGs in
        # Talaria/Resources/AppIcons/ (Icon-<Name>@2x/@3x.png). Adding an icon =
        # a new key here + its PNGs + an AppIconCatalog entry, then
        # `xcodegen generate`. Keys are the names passed to setAlternateIconName.
        CFBundleIcons:
          CFBundlePrimaryIcon:
            CFBundleIconName: AppIcon
          CFBundleAlternateIcons:
            DeepField:
              CFBundleIconFiles:
                - Icon-DeepField
              UIPrerenderedIcon: false
            SolarForge:
              CFBundleIconFiles:
                - Icon-SolarForge
              UIPrerenderedIcon: false
            Terminal:
              CFBundleIconFiles:
                - Icon-Terminal
              UIPrerenderedIcon: false
            PaperTape:
              CFBundleIconFiles:
                - Icon-PaperTape
              UIPrerenderedIcon: false
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
        UIBackgroundModes:
          - processing
          - location
          - audio
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: true
          UISceneConfigurations:
            CPTemplateApplicationSceneSessionRoleApplication:
              - UISceneClassName: CPTemplateApplicationScene
                UISceneConfigurationName: CarPlay
                UISceneDelegateClassName: "$(PRODUCT_MODULE_NAME).CarPlaySceneDelegate"

  TalariaWidgets:
    type: app-extension
    platform: iOS
    sources:
      - path: TalariaWidgets
      # Shared theme palette tables (also compiled into the app target).
      - path: Shared
    entitlements:
      path: TalariaWidgets/TalariaWidgets.entitlements
      properties:
        com.apple.security.application-groups:
          - group.io.github.eulric90.talaria
    info:
      path: TalariaWidgets/Info.plist
      properties:
        CFBundleDisplayName: Talaria Widgets
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.github.eulric90.talaria.Widgets
        PRODUCT_NAME: TalariaWidgets
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "6.2"
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: ""  # set to your own free Apple ID personal-team ID if building signed locally in Xcode
        TARGETED_DEVICE_FAMILY: "1,2"
    scheme: {}

  TalariaTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: TalariaTests
    dependencies:
      - target: Talaria
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.github.eulric90.talaria.tests
        TEST_TARGET_NAME: Talaria
        # PRODUCT_NAME on the app target is "Talaria for Hermes Desktop", so
        # XcodeGen's auto-derived TEST_HOST (Talaria.app/Talaria) is wrong.
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Talaria for Hermes Desktop.app/Talaria for Hermes Desktop"
        BUNDLE_LOADER: "$(TEST_HOST)"
        GENERATE_INFOPLIST_FILE: true

  TalariaUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: TalariaUITests
    dependencies:
      - target: Talaria
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.github.eulric90.talaria.uitests
        TEST_TARGET_NAME: Talaria
        GENERATE_INFOPLIST_FILE: true
